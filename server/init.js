Meteor.startup(function () {
    // if(!Meteor.roles.findOne({name: "cmsinn_admin"})){
    //     Roles.createRole("cmsinn_admin");
    // }

    // if (Meteor.users.findOne(Meteor.settings.root)){
    //     Roles.addUsersToRoles(Meteor.settings.root, ['cmsinn_admin']);
    // }
});